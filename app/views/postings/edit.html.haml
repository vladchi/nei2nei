%h1 Editing posting

= error_messages_for :posting

- form_for(:posting, :url => object_url, :html => { :method => :put , :multipart => true }) do |f|
  = render :partial => "form", :locals => {:f => f}
  #google_maps_box= render :partial => "/postings/googlemaps", :locals => {:f => f}
  %p= submit_tag "Update"

= link_to 'Show', object_path
|
= link_to 'Back', objects_path
