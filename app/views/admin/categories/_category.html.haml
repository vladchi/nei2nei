%div[category]
  = h category.name
  |
  = link_to 'Edit', edit_admin_category_path(category)
  |
  = link_to 'Destroy', admin_category_path(category), :confirm => 'Really destroy category?', :method => :delete
