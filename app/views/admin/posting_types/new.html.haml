%h1 Creating posting_type

= error_messages_for :posting_type

- form_for(:posting_type, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', admin_posting_types_path
