%h1 Editing posting_type

= error_messages_for :posting_type

- form_for(:posting_type, :url => object_url, :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Update"


= link_to 'Back', admin_posting_types_path
